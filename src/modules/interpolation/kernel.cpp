#include "kernel.hpp"
#include <stdio.h>

namespace oa
{
	namespace kernel
	{	
/*		
		ArrayPtr kernel_interpolation(vector<ArrayPtr> &ops_ap)
		{
			ArrayPtr A = ops_ap[0];
					
			int u_dt = A->get_data_type();
					
			switch(u_dt)
			{
				case DATA_INT:
					return t_kernel_interpolation<int>(ops_ap);
					//break;
				case DATA_DOUBLE:
					return t_kernel_interpolation<double>(ops_ap);
					//break;
				case DATA_FLOAT:
					return t_kernel_interpolation<float>(ops_ap);
					//break;
			}
		}
*/
	}
}
